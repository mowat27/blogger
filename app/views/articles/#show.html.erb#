<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<br />
<%= link_to "Edit", edit_article_path(@article) %>

<br/>
<%= link_to "Delete Article", article_path(@article),
      :method => :delete, :confirm =>  "Do you really want to delete #{@article.title}?"%>

<br/>

<h2><%= @article.comments.count %> Comments</h2>

<%= render partial: "comment", collection: @article.comments %>


<%= render "comment_form", :article => @article, :comment => @comment %>
<%= link_to "Back to Articles", articles_path %>
