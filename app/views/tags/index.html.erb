<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li> <%= link_to tag.name, tag %> (<%= tag.articles.count %>)
  <%= link_to "[del]", tag_path(tag), :method => :delete %> </li>
  <% end %>
</ul>
